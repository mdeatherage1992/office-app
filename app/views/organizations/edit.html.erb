<h1>Edit Organization</h1>

<%= form_for(@org) do |f| %>
<%= f.label "Name" %>
<%= f.text_field :name, placeholder: @org.name %>
<%= f.number_field :rate, placeholder: @org.rate %>
<%= f.submit "Update Organization" %>
<% end %>
